import { StyleSheet, Text, View } from "react-native";
import { theme } from "./theme";

export function LeaderboardRow({ rank, initials, name, stat, score, scoreLabel, movement, isYou = false }: { rank: number; initials: string; name: string; stat: string; score: number; scoreLabel?: string; movement: number; isYou?: boolean }) {
  const winner = rank === 1;
  return <View style={[styles.row, isYou && styles.you]}><View style={[styles.rank, winner && styles.rankWinner, isYou && styles.rankYou]}><Text style={[styles.rankText, isYou && styles.rankTextYou]}>{rank}</Text></View><View style={styles.avatar}><Text style={styles.initials}>{initials}</Text></View><View style={styles.copy}><Text style={styles.name}>{name}</Text><Text style={styles.stat}>{stat}</Text></View><View style={styles.score}><Text style={styles.scoreValue}>{score.toLocaleString()}{scoreLabel ?? ""}</Text><Text style={[styles.movement, movement < 0 && styles.down]}>{movement === 0 ? "—" : `${movement > 0 ? "▲" : "▼"} ${Math.abs(movement)}`}</Text></View></View>;
}
const styles = StyleSheet.create({ row: { minHeight: 72, flexDirection: "row", alignItems: "center", gap: 10, paddingHorizontal: 12, paddingVertical: 10, borderRadius: 14, borderWidth: 1, borderColor: theme.colors.border, backgroundColor: theme.colors.surface }, you: { borderWidth: 2, borderColor: theme.colors.brand, backgroundColor: theme.colors.subtle }, rank: { width: 32, height: 32, alignItems: "center", justifyContent: "center", borderRadius: 16, backgroundColor: theme.colors.subtle }, rankWinner: { backgroundColor: theme.colors.accent }, rankYou: { backgroundColor: theme.colors.brand }, rankText: { color: theme.colors.text, fontFamily: theme.type.body, fontWeight: "800", fontSize: 13 }, rankTextYou: { color: "#fff" }, avatar: { width: 40, height: 40, alignItems: "center", justifyContent: "center", borderRadius: 20, backgroundColor: theme.colors.subtle }, initials: { color: theme.colors.text, fontFamily: theme.type.body, fontWeight: "700", fontSize: 12 }, copy: { flex: 1 }, name: { color: theme.colors.text, fontFamily: theme.type.body, fontWeight: "700", fontSize: 14 }, stat: { color: theme.colors.textSecondary, fontFamily: theme.type.body, fontSize: 13 }, score: { width: 76, alignItems: "flex-end" }, scoreValue: { color: theme.colors.text, fontFamily: theme.type.body, fontWeight: "700", fontSize: 20 }, movement: { color: theme.colors.success, fontFamily: theme.type.body, fontWeight: "700", fontSize: 10 }, down: { color: theme.colors.danger } });
