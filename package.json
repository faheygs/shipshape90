{
  "name": "shipshape90",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=24.3.0"
  },
  "scripts": {
    "dev:mobile": "pnpm --filter @shipshape/mobile start",
    "dev:web": "pnpm --filter @shipshape/web dev",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r --if-present test",
    "lint": "pnpm -r --if-present lint"
  },
  "devDependencies": {
    "supabase": "^2.111.0",
    "typescript": "~6.0.3",
    "vitest": "^4.1.10"
  }
}
